export enum ComponentFilterType {
  ALL = 0,
  INPUT = 1,
  DISPLAY = 2,
  FEEDBACK = 3,
  NAVIGATION = 4,
}

export enum ComponentSortCondition {
  NAME_ASC = "이름 순으로 정렬",
  VIEW_DESC = "조회 순으로 정렬",
  COMMENT_DESC = "댓글 순으로 정렬",
}
